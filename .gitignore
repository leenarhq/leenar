node_modules/
.DS_Store
dist/
.env
.env.*
.dev.vars
.dev.vars.*
.agents/
!.env.example
*.local
.wrangler/
*.tsbuildinfo
test-results.xml
workers/api/coverage/
.tanstack/
secrets.md
supabase/.temp/
path.md

# Local tooling & generated reports
.claude/
.seo-agent/
seo_*.md
seo_*.json

# Generated validation-harness dumps (regenerable via .validate/validate.sh);
# keep shim.sql and validate.sh tracked.
supabase/core-migrations/.validate/*.sql
!supabase/core-migrations/.validate/shim.sql

# Local tooling scratch (gstack browse logs) — machine-specific, never shipped.
.gstack/

# Demo video — hosted on GitHub user-attachments, linked from README. Never commit the file.
leenar-demo.mp4

# Marketing drafts — working notes, never shipped and never public.
# The leading slash is load-bearing: it pins this to the repo root. A bare
# `content/` matches at every depth, so it would also swallow the nested
# content/ directories elsewhere in the tree — already-tracked files survive,
# but every NEW file added under one becomes invisible to `git add`.
/content/
